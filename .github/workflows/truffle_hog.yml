on:
  pull_request:

jobs:
  TruffleHog:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: TruffleHog OSS
        id: trufflehog
        uses: trufflesecurity/trufflehog@65725840a041bf31aec4473016b803900070127d # v3.95.0
        continue-on-error: true
        with:
          path: ./

      - name: Scan Results Status
        if: steps.trufflehog.outcome == 'failure'
        run: exit 1
